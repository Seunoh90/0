export interface Character {
  id: string;
  name: string;
  age: number;
  height: string;
  shortDesc: string;
  themeColor: string;
  images: string[];
  description: string[];
  roomName: string;
  roomDesc: string;
}

export interface PalaceInfo {
  name: string;
  desc: string;
}